Q 1a git commit
    git init

   b.1.help the projects to work efficient together

     2.for security and authorization

   c.1.to checkout the developed works in master from the file developed

	2. .git pull
	   .git checkout -m


      